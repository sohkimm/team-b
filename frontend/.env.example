# 프론트가 호출할 백엔드 베이스 URL.
# 로컬: 비워두면 코드 기본값(http://localhost:8000) 사용. 명시하려면 아래처럼.
# 배포(Vercel): 대시보드 Environment Variables 에 Render 백엔드 주소를 넣는다.
#   예) VITE_API_URL=https://ncval-backend.onrender.com
VITE_API_URL=http://localhost:8000
